# Task 02 — Add specs

Copy/paste the whole block below into Codex (VS Code) as a single instruction.

---

Task: Add format specs (no runtime changes).

Ensure the following files exist and are committed:
- docs/spec/pct2.md
- docs/spec/hierarchy_paging.md
- docs/spec/edit_patches.md

Do not modify application runtime code yet.

Acceptance:
- npm run lint passes
- npm run build passes
- Demo dataset renders and hover/click picking still works
- No breaking type errors (TypeScript)
